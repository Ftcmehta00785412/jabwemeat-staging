import React from 'react';
import { Minus, Plus, Star, Snowflake } from 'lucide-react';
import type { Product } from '../types';

const PRODUCT_IMAGES:Record<string,string>={p1:'assets/chicken-curry.webp',p2:'assets/chicken-breast.webp',p3:'assets/mutton.webp',p4:'assets/rohu.webp',p5:'assets/prawns.webp',p6:'assets/eggs.webp',p7:'assets/tikka.webp',p8:'assets/family-combo.webp'};
type Props={product:Product; quantity:number; onAdd:()=>void; onRemove:()=>void};
export const ProductCard: React.FC<Props> = ({product,quantity,onAdd,onRemove}) => {const discount=Math.round((1-product.price/product.mrp)*100);return <article className="product-card"><div className="product-image-wrap"><img src={product.imageUrl || PRODUCT_IMAGES[product.id] || 'assets/chicken-curry.webp'} alt={product.name}/><span className="discount-pill">SAVE {discount}%</span>{product.featured&&<span className="bestseller"><Star fill="currentColor"/> BEST SELLER</span>}</div><div className="product-info"><p className="product-category">{product.category}</p><h3>{product.name}</h3><div className="product-rating"><span>★★★★★</span><small>Fresh pick</small></div><div className="product-meta"><span className="meta-tag weight-tag">{product.weight}</span><span className="meta-tag cut-tag">{product.category} cut</span><span className="meta-tag serving-tag">Serves {product.servings}</span></div><div className="fresh-note"><Snowflake/> Freshly packed for your slot</div><div className="product-buy-row"><div className="price"><strong>₹{product.price}</strong><del>₹{product.mrp}</del></div>{quantity===0?<button className="add-button" onClick={onAdd} disabled={product.stock===0}>{product.stock===0?'SOLD OUT':'ADD TO CART'}</button>:<div className="quantity-control"><button onClick={onRemove}><Minus/></button><span>{quantity}</span><button onClick={onAdd}><Plus/></button></div>}</div></div></article>};
